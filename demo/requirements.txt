# Minimal dependencies for demo/demo_app.py: it only replays precomputed
# ../demo_data/ (video, JSON, CSV) - no YOLO, no Gemini API, no GPU needed.
# Lives here (not the repo root) so Streamlit Community Cloud picks this up
# instead of the heavy root requirements.txt (torch/ultralytics/etc).
streamlit==1.51.0
pandas==2.3.3
plotly==6.5.0
